backend:
  name: github
  repo: robbie-likescodes/Tessa-Bennion-s-Art-Page
  branch: main
  # For private repos you must configure an OAuth app or proxy (Decap docs).

media_folder: "uploads"     # where images are committed in the repo
public_folder: "uploads"    # relative URLs so project Pages work

publish_mode: editorial_workflow

collections:
  - name: "works"
    label: "Works"
    folder: "content/works"      # create this folder (add a .gitkeep)
    create: true
    slug: "{{year}}-{{slug}}"
    editor:
      preview: false
    fields:
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Year", name: "year", widget: "number"}
      - {label: "Medium", name: "medium", widget: "string"}
      - {label: "Dimensions", name: "dimensions", widget: "string"}
      - {label: "Cover Image", name: "cover", widget: "image"}
      - label: "Gallery"
        name: "images"
        widget: "list"
        field: {label: "Image", name: "image", widget: "image"}
      - {label: "Description", name: "body", widget: "markdown", required: false}
      - {label: "Tags", name: "tags", widget: "list", required: false}
